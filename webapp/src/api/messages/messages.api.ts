import sendMessage from "@/api/messages/actions/sendMessage.action";
import sendFile from "@/api/messages/actions/sendFiles.action";

const messages = {
    sendMessage,
    sendFile
}

export default messages;